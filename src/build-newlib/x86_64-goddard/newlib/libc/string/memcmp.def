@node memcmp
@section @code{memcmp}---compare two memory areas
@findex memcmp
@strong{Synopsis}
@example
#include <string.h>
int memcmp(const void *@var{s1}, const void *@var{s2}, size_t @var{n});

@end example
@strong{Description}@*
This function compares not more than @var{n} characters of the
object pointed to by @var{s1} with the object pointed to by @var{s2}.

@*
@strong{Returns}@*
The function returns an integer greater than, equal to or
less than zero 	according to whether the object pointed to by
@var{s1} is greater than, equal to or less than the object
pointed to by @var{s2}.

@*
@strong{Portability}@*
@code{memcmp} is ANSI C.

@code{memcmp} requires no supporting OS subroutines.

@*
