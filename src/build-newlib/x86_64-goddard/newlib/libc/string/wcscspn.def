@node wcscspn
@section @code{wcscspn}---get length of a complementary wide substring 
@strong{Synopsis}
@example
#include <wchar.h>
size_t wcscspn(const wchar_t *@var{s}, wchar_t *@var{set});

@end example
@strong{Description}@*
The @code{wcscspn} function computes the length of the maximum initial
segment of the wide-character string pointed to by @var{s} which consists
entirely of wide-character codes not from the wide-character string
pointed to by @var{set}. 

@*
@strong{Returns}@*
The @code{wcscspn} function returns the length of the initial substring of
@var{s1}; no return value is reserved to indicate an error.

@*
@strong{Portability}@*
@code{wcscspn} is ISO/IEC 9899/AMD1:1995 (ISO C).

No supporting OS subroutines are required.

@*
