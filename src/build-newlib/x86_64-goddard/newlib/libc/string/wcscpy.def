@node wcscpy
@section @code{wcscpy}---copy a wide-character string 
@strong{Synopsis}
@example
#include <wchar.h>
wchar_t *wcscpy(wchar_t *__restrict @var{s1},
    const wchar_t *__restrict ,@var{s2});

@end example
@strong{Description}@*
The @code{wcscpy} function copies the wide-character string pointed to by
@var{s2} (including the terminating null wide-character code) into the
array pointed to by @var{s1}. If copying takes place between objects that
overlap, the behaviour is undefined. 

@*
@strong{Returns}@*
The @code{wcscpy} function returns @var{s1}; no return value is reserved to
indicate an error. 

@*
@strong{Portability}@*
@code{wcscpy} is ISO/IEC 9899/AMD1:1995 (ISO C).

No supporting OS subroutines are required.

@*
