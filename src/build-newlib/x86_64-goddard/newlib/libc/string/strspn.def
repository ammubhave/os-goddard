@node strspn
@section @code{strspn}---find initial match
@findex strspn
@strong{Synopsis}
@example
#include <string.h>
size_t strspn(const char *@var{s1}, const char *@var{s2});

@end example
@strong{Description}@*
This function computes the length of the initial segment of
the string pointed to by @var{s1} which consists entirely of
characters from the string pointed to by @var{s2} (excluding the
terminating null character).

@*
@strong{Returns}@*
@code{strspn} returns the length of the segment found.

@*
@strong{Portability}@*
@code{strspn} is ANSI C.

@code{strspn} requires no supporting OS subroutines.

@*
